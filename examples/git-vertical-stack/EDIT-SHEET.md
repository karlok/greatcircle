# Git, stacked : edit sheet

REGENERATED FROM THE LIVE DECK. Every edit already applied is in here, and
every beat that has been restructured is shown as it now actually plays.

Some beats are built from several key presses. Those are broken out below as
"press 1 of 3" and so on: each press has its own voice-over line and its own
reveal, and each one is a separate → on the night.

Every beat, in order. Edit the text in place; leave the `[id]` markers alone.
Delete nothing you want kept. Add a line starting with `>>` anywhere for a note
back to the author (e.g. `>> swap this photo`, `>> cut this beat`, `>> hold longer`).

Beat numbers match the counter in the bottom-right corner of the screen.

12 beats · 18 presses · 12 nodes

---

## 01 / 12  ·  Prologue

VOICE OVER:
  [title]

ON CANVAS:
  [t.title]
    Git, stacked
    ASIDE: one axis, three ideas

---

## 02 / 12  ·  Prologue

VOICE OVER:
  Here's the pitch: skip the timeline entirely. A commit just stacks on top of the one before it.

ON CANVAS:
  [t.premise]
    Every commit lands on top of the last one.
    ASIDE: No timeline. No left and right. Just a pile that only ever grows.

---

## 03 / 12  ·  I · The stack

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    One commit. main points at it — that's all a branch name is, a label on a card.

  ON CANVAS:
    [g.s0]
      c1
      c2
      main
      CAPTION: one commit — main points at itcommit again, and main moves up with you

  ── press 2 of 2 ──

  VOICE OVER:
    Commit again, and watch main. It doesn't stay put, it moves up to whatever's newest.

  APPEARS NOW:
    [g.s0__to]  (the diagram [g.s0] animates to its second state)
      NOW READS: commit again, and main moves up with you

---

## 04 / 12  ·  I · The stack

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    Same pile, one card up.

  ON CANVAS:
    [g.s1]
      c1
      c2
      c3
      main
      CAPTION: main, one commit upand again. the stack just grows

  ── press 2 of 2 ──

  VOICE OVER:
    And again. There's no new mechanic here — that's kind of the point. Committing is just this, repeated.

  APPEARS NOW:
    [g.s1__to]  (the diagram [g.s1] animates to its second state)
      NOW READS: and again. the stack just grows

---

## 05 / 12  ·  II · Off to the side

VOICE OVER:
  Now two people want to work at the same time, without stepping on each other.

ON CANVAS:
  [t.branch]
    Now two people want to work at once.
    ASIDE: A branch doesn't fork the timeline. It just starts a second, shorter pile — tied to the card it grew out of.

---

## 06 / 12  ·  II · Off to the side

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    Main's pile, three cards deep.

  ON CANVAS:
    [g.s2]
      c1
      c2
      c3
      f1
      main
      feature
      CAPTION: three commits, one columnbranch: a new commit, one column over — still tied to where it started

  ── press 2 of 2 ──

  VOICE OVER:
    Branching doesn't split this timeline, because there isn't one. It starts a second, shorter pile — one column over — and that first card stays tied back to exactly where it grew out of.

  APPEARS NOW:
    [g.s2__to]  (the diagram [g.s2] animates to its second state)
      NOW READS: branch: a new commit, one column over — still tied to where it started

---

## 07 / 12  ·  II · Off to the side

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    feature, one commit.

  ON CANVAS:
    [g.s3]
      c1
      c2
      c3
      f1
      f2
      main
      feature
      CAPTION: feature has its own commitand it stacks too, in its own column

  ── press 2 of 2 ──

  VOICE OVER:
    And it stacks too, same as main does — just in its own column, on its own schedule.

  APPEARS NOW:
    [g.s3__to]  (the diagram [g.s3] animates to its second state)
      NOW READS: and it stacks too, in its own column

---

## 08 / 12  ·  III · Folding back in

VOICE OVER:
  Eventually that side pile has to come back.

ON CANVAS:
  [t.merge]
    Merging isn't a special move.
    ASIDE: It's one more commit — the only kind with two parents.

---

## 09 / 12  ·  III · Folding back in

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    Two piles, side by side, both still growing.

  ON CANVAS:
    [g.s4]
      c1
      c2
      c3
      f1
      f2
      m
      main
      feature
      CAPTION: two piles, side by sidemerge: one commit, two parents, folded back into main

  ── press 2 of 2 ──

  VOICE OVER:
    A merge is one more commit — the only kind with two parents instead of one. It folds the side pile back into main's column, and main moves up onto it.

  APPEARS NOW:
    [g.s4__to]  (the diagram [g.s4] animates to its second state)
      NOW READS: merge: one commit, two parents, folded back into main

---

## 10 / 12  ·  IV · The turn

THIS BEAT IS 2 PRESSES. Each one is a separate → on the night.

  ── press 1 of 2 ──

  VOICE OVER:
    feature is still labelled, sitting right there next to main.

  ON CANVAS:
    [g.s5]
      c1
      c2
      c3
      f1
      f2
      m
      main
      feature
      CAPTION: feature is still labelleddelete the branch — the label goes. the commits underneath do not

  ── press 2 of 2 ──

  VOICE OVER:
    Delete the branch, and watch what actually disappears — the label. Not the cards. They're still in the pile, they just don't have a name pointing at them anymore.

  APPEARS NOW:
    [g.s5__to]  (the diagram [g.s5] animates to its second state)
      NOW READS: delete the branch — the label goes. the commits underneath do not

---

## 11 / 12  ·  IV · The turn

VOICE OVER:
  Which is the whole model, really. Nothing gets thrown away. main is a sticky note, not a box.

ON CANVAS:
  [g.s5]
    c1
    c2
    c3
    f1
    f2
    m
    main
    feature
    CAPTION: feature is still labelleddelete the branch — the label goes. the commits underneath do not
  [g.s5__to]  (the diagram [g.s5] animates to its second state)
    NOW READS: delete the branch — the label goes. the commits underneath do not
  [t.turn]
    Nothing in that pile was ever thrown away.
    ASIDE: main was never a container. It's a sticky note on whichever card is currently on top.

---

## 12 / 12  ·  IV · The turn

VOICE OVER:
  this is the beat to stop on and actually ask the room

ON CANVAS:
  [g.s5]
    c1
    c2
    c3
    f1
    f2
    m
    main
    feature
    CAPTION: feature is still labelleddelete the branch — the label goes. the commits underneath do not
  [g.s5__to]  (the diagram [g.s5] animates to its second state)
    NOW READS: delete the branch — the label goes. the commits underneath do not
  [t.turn]
    Nothing in that pile was ever thrown away.
    ASIDE: main was never a container. It's a sticky note on whichever card is currently on top.
  [t.close]
    Does the pile read faster than the timeline did?
    ASIDE: That's the actual question this deck exists to answer.

---
