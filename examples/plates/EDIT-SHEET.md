# Plates : edit sheet

REGENERATED FROM THE LIVE DECK. Every edit already applied is in here, and
every beat that has been restructured is shown as it now actually plays.

Some beats are built from several key presses. Those are broken out below as
"press 1 of 3" and so on: each press has its own voice-over line and its own
reveal, and each one is a separate → on the night.

Every beat, in order. Edit the text in place; leave the `[id]` markers alone.
Delete nothing you want kept. Add a line starting with `>>` anywhere for a note
back to the author (e.g. `>> swap this photo`, `>> cut this beat`, `>> hold longer`).

Beat numbers match the counter in the bottom-right corner of the screen.

8 beats · 8 presses · 8 nodes

---

## 01 / 8  ·  Plates

VOICE OVER:
  [title]

ON CANVAS:
  [t.title]
    Plates
    ASIDE: Six photographs, and a bug that only happens in a real browser

---

## 02 / 8  ·  Plates

VOICE OVER:
  One plate, framed close. The easy case.

ON CANVAS:
  [p.harbour]   photo: harbour
    CAPTION: A harbour, 2600 &times; 1750

---

## 03 / 8  ·  Plates

VOICE OVER:
  A second, revealed on a wider shot.

ON CANVAS:
  [p.ridge]   photo: ridge
    CAPTION: Revealed on a wide shot, which is when the stall used to bite

---

## 04 / 8  ·  The unkind part

VOICE OVER:
  A long flight across the canvas to a cold plate. This is the one that used to stall.

ON CANVAS:
  [p.crossing]   photo: crossing
    CAPTION: A long flight from the last one, on purpose

---

## 05 / 8  ·  The unkind part

VOICE OVER:
  All the way back the other way, and rotated.

ON CANVAS:
  [p.archive]   photo: archive
    CAPTION: Rotated, because the fit maths has to cope

---

## 06 / 8  ·  The unkind part

VOICE OVER:
  Two large rasters on one press, on a wide shot.

ON CANVAS:
  [p.delta]   photo: delta
    CAPTION: Two large rasters revealed on the same press
  [p.beacon]   photo: beacon
    CAPTION: And the last one, framed tight

---

## 07 / 8  ·  The unkind part

VOICE OVER:
  And the instruction for whoever tests this next.

ON CANVAS:
  [t.how]
    If any photograph here ever paints half-drawn, the pre-decode fix has
    regressed.
    ASIDE: Press R to force a repaint. Needing to is the bug.

---

## 08 / 8  ·  The unkind part

VOICE OVER:
  [everything at once, which is the worst case for the compositor]

ON CANVAS:
  (camera move only)

---
